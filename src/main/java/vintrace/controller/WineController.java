package vintrace.controller;


/*
@RestController
@RequestMapping("/api/v1/wine")
public class WineController {
    @GetMapping("/{wineId}")
    public String getWideDetails(@PathVariable(value = "wineId") Long wineId) {
        return "Success: " + wineId;
    }

    @GetMapping("/{wineId}/breakdown/{type}")
    public String getBreakdown(@PathVariable(value = "wineId") Long wineId, @PathVariable(value = "type") String breakdownType) {
        return "Success: " + wineId + " Breakdown Type: " + breakdownType;
    }

    @GetMapping("/productstate/{stateId}")
    public String getWinesByState(@PathVariable(value = "stateId") Long stateId) {
        return "Success: " + stateId;
    }


    @GetMapping("/owner/{ownerId}")
    public String getWinesByOwner(@PathVariable(value = "ownerId") Long ownerId) {
        return "Success: " + ownerId;
    }
}
*/
